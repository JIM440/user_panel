// Reviews Page

1. change icon in review table from dots to delete icon. (also when something is deleted, the state is updated and the database is updated).
2. For sorting products in by newest/ latest, just use array.prototype.sort (no need for a route).
3. In short for reviews page, once the route for all reviews is gotten, the rest of the filters can be gotten by js manipulation.
firstly set the data to a variable called fetchedData, then there's a state which will hold the data to be displayed based on the filter. When a filter is changed data is filtered using .filter to take the value of that filter and when in default data is fetchedData.
4. You can search by reviewer, product name

// Orders Page

1. you can seach by customer, payment method and location
2. also for filtering and searching, do same as reviews page
3.